{
  "name": "tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test:integration": "playwright test",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
